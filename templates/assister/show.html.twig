{% extends 'base.html.twig' %}

{% block title %}Assister{% endblock %}

{% block body %}
    <h1>Assister</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ assister.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('assister_index') }}">back to list</a>

    <a href="{{ path('assister_edit', {'id': assister.id}) }}">edit</a>

    {{ include('assister/_delete_form.html.twig') }}
{% endblock %}
