<!doctype html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">
        <title>S'identifier | S'inscrire</title>
        {{ encore_entry_link_tags('app') }}
            <style>
                /* ----------------------------- Import fonts from google font ----------------------------- */
                @import url('https://fonts.googleapis.com/css2?family=Lobster&family=Montserrat:wght@200;300;400;500;600;700;900&family=Open+Sans:wght@300;400;600;700;800&family=Roboto:wght@300;400;700;900&display=swap');
                /* -------------------- Global classes -------------------- */
                :root{
                    --color-green: #009579;
                    --color-green-glossy: #0affd2;
                    --primary-gradient: linear-gradient(to right, #4e4376, #2b5876);
                    --OpenSans: 'Open Sans', sans-serif;
                    --Montserrat: 'Montserrat', sans-serif;
                    --Lobster: 'Lobster', sans-serif;
                    --Roboto: 'Roboto', sans-serif;
                }

                *{
                    padding: 0;
                    margin: 0;
                    box-sizing: border-box;
                }

                body, input{
                    font-family: var(--Montserrat);
                }
                /* ----------x--------- Global classes ----------x--------- */
                .container{
                    padding: 0;
                    margin: 0;
                    position: relative;
                    min-width: 100%;
                    min-height: 100vh;
                    background-color: #ffffff;
                    overflow: hidden;
                }

                .forms-container{
                    position: absolute;
                    width: 100%;
                    height: 100%;
                    top: 0;
                    left: 0;
                }

                form{
                    display: flex;
                    flex-direction: column;
                    align-items: center;
                    justify-content: center;
                    grid-column: 1 / 2;
                    grid-row: 1 / 2;
                    padding: 0 5rem;
                    overflow: hidden;
                    transition: 0.2s 0.7s ease-in-out;
                }

                .title{
                    color: #444;
                    margin-bottom: 10px;
                }

                .input-field{
                    max-width: 380px;
                    width: 100%;
                    height: 55px;
                    background-color: #f0f0f0;
                    margin: 10px 0;
                    border-radius: 55px;
                    display: grid;
                    grid-template-columns: 15% 85%;
                    padding: 0 0.4rem;
                }

                .input-field i{
                    text-align: center;
                    line-height: 55px;
                    color: #acacac;
                    font-size: 1.1rem;
                }

                .input-field input{
                    background: none;
                    border: none;
                    outline: none;
                    line-height: 1;
                    font-size: 1.1rem;
                    font-weight: 600;
                    color: #333;
                }

                .input-field input::placeholder{
                    color: #aaa;
                    font-weight: 500;
                }

                .btn{
                    width: 150px;
                    height: 49px;
                    border: none;
                    outline: none;
                    border-radius: 49px;
                    cursor: pointer;
                    background: var(--primary-gradient);
                    color: #ffffff;
                    /* text-transform: uppercase; */
                    /* font-weight: 600; */
                    margin: 10px 0;
                }

                .btn:hover{
                    opacity: 0.85;
                    color: #ffffff;
                    transition: all ease-in-out 200ms;
                }

                .social-text{
                    padding: 0.7rem 0;
                    font-size: 1rem;
                }

                .social-media{
                    display: flex;
                    justify-content: center;
                }

                .social-media a{
                    outline: none;
                }

                .social-icon{
                    height: 46px;
                    width: 46px;
                    border: 1px solid #333;
                    margin: 0 0.45rem;
                    display: flex;
                    justify-content: center;
                    align-items: center;
                    text-decoration: none;
                    color: #333;
                    font-size: 1.3rem;
                    border-radius: 50%;
                }

                .facebook:hover{
                    color: #1877f2;
                    text-decoration: none;
                }
                .facebook i{
                    transition: all ease-in-out 150ms;
                }
                .twitter:hover{
                    color: #1da1f2;
                    text-decoration: none;
                }
                .twitter i {
                    transition: all ease-in-out 150ms;
                }
                .google:hover{
                    text-decoration: none;
                    color: transparent;
                    /* background: radial-gradient(circle at 90% 90%, #0f9d58 20%, #4285f4 50%, #f4b400 45%, #db4437 40%); */
                    /*background: conic-gradient(from 45deg, #4285f4, #0f9d58, #f4b400, #db4437); */
                    /*background: conic-gradient(#db4437, #4285f4, #0f9d58, #f4b400);*/
                    background: conic-gradient(from 75deg, #4285f4 50grad, #0f9d58 0deg 180deg, #f4b400 0deg 240deg, #db4437 20%);
                    -webkit-background-clip: text;
                }
                .google i {
                    transition: all ease-in-out 150ms;
                }
                .linkedin:hover{
                    color: #0a66c2;
                    text-decoration: none;
                }
                .linkedin i {
                    transition: all ease-in-out 150ms;
                }

                .signin-signup{
                    position: absolute;
                    top: 50%;
                    left: 75%;
                    transform: translate(-50%, -50%);
                    width: 50%;
                    display: grid;
                    grid-template-columns: 1fr;
                    z-index: 5;
                    transition: 1s 0.7s ease-in-out;
                }/* J'ajoute  grid-column et grid-row dans form */

                form.sign-in-form{
                    z-index: 2;
                }

                form.sign-up-form{
                    z-index: 1;
                    opacity: 0;
                }/* J'ajoute padding et overflow dans form */

                .container:before{
                    content: '';
                    position: absolute;
                    width: 2350px;
                    height: 2350px;
                    border-radius: 50%;
                    background: #fdbd12;
                    top: -10%;
                    right: 48%;
                    transform: translateY(-50%);
                    z-index: 6;
                    transition: 1.8s ease-in-out;
                }

                .image{
                    width: 100%;
                    transition: 1.1s 0.4s ease-in-out;
                }

                .panels-container{
                    position: absolute;
                    width: 100%;
                    height: 100%;
                    top: 0;
                    left: 0;
                    /* background-color: green; */
                    display: grid;
                    grid-template-columns: repeat(2, 1fr);
                }

                .panel{
                    display: flex;
                    flex-direction: column;
                    align-items: flex-end;
                    justify-content: space-around;
                    text-align: center;
                    z-index: 7;
                }

                .left-panel{
                    padding: 3rem 17% 2rem 12%;
                    pointer-events: all;
                }

                .right-panel{
                    padding: 3rem 12% 2rem 17%;
                    pointer-events: none;
                }

                .panel .content{
                    color: #ffffff;
                    transition: 0.9s 0.6s ease-in-out;
                }

                .panel h3{
                    font-weight: 600;
                    line-height: 1;
                    font-size: 1.5rem;
                }

                .panel p{
                    font-size: 0.95rem;
                    padding: 0.7rem 0;
                }

                .btn.transparent{
                    margin: 0;
                    background: none;
                    border: 2px solid #ffffff;
                    width: 130px;
                    height: 41px;
                    font-weight: 500;
                    font-size: 0.8rem;
                }

                .right-panel .content, .right-panel .image{
                    transform: translateX(800px);
                }/* J'ajoute z'index dans panel, dans container:before et dans signin-signup | dans left-panel & right-panel, j'ajoute pointer-events */

                /* -------------------------- Animation -------------------------- */
                .container.sign-up-mode:before{
                    transform: translate(100%, -50%);
                    right: 52%;
                }

                .container.sign-up-mode .left-panel .image,
                .container.sign-up-mode .left-panel .content{
                    transform: translateX(-800px);
                }

                .container.sign-up-mode .right-panel .content,
                .container.sign-up-mode .right-panel .image{
                    transform: translateX(0px);
                }

                .container.sign-up-mode .left-panel{
                    pointer-events: none;
                }

                .container.sign-up-mode .right-panel{
                    pointer-events: all;
                }

                .container.sign-up-mode .signin-signup{
                    left: 25%;
                }

                .container.sign-up-mode form.sign-in-form{
                    z-index: 1;
                    opacity: 0;
                }

                .container.sign-up-mode form.sign-up-form{
                    z-index: 2;
                    opacity: 1;
                }
                /* -------------x------------ Animation -------------x------------ */
                /* --------------------------- Responsive page ---------------------------  */
                @media (max-width: 1024px) {
                    .container{
                        min-height: 800px;
                        height: 100vh;
                    }

                    .container:before{
                        width: 1500px;
                        height: 1500px;
                        left: 30%;
                        bottom: 68%;
                        transform: translateX(-50%);
                        right: initial;
                        top: initial;
                        transition: 2s ease-in-out;
                    }

                    .signin-signup{
                        width: 100%;
                        left: 50%;
                        top: 95%;
                        transform: translate(-50%, -100%);
                        transition: 1s 0.8s ease-in-out;
                    }

                    .panels-container{
                        grid-template-columns: 1fr;
                        grid-template-rows: 1fr 2fr 1fr;
                    }

                    .right-panel .content, .right-panel .image{
                        transform: translateY(300px);
                    }

                    .panel{
                        flex-direction: row;
                        justify-content: space-around;
                        align-items: center;
                        padding: 2.5rem 8%;
                    }

                    .panel .content{
                        padding-right: 15%;
                        transition: 0.9s, 0.8s ease-in-out;
                    }

                    .panel h3{
                        font-size: 1.2rem;
                    }

                    .panel p{
                        font-size: 0.7rem;
                        padding: 0.5rem 0;
                    }

                    .btn.transparent{
                        width: 110px;
                        height: 35px;
                        font-size: 0.7rem;
                    }

                    .image{
                        width: 200px;
                        transition: 1.5s, 1.5s ease-in-out;
                    }

                    .left-panel{
                        grid-row: 1 / 2;
                    }

                    .right-panel{
                        grid-row: 3 / 4;
                    }

                    .container.sign-up-mode:before{
                        transform: translate(-50%, 100%);
                        bottom: 32%;
                        right: initial;
                    }

                    .container.sign-up-mode .left-panel .image,
                    .container.sign-up-mode .left-panel .content{
                        transform: translateY(-300px);
                    }

                    .container.sign-up-mode .signin-signup{
                        top: 5%;
                        transform: translate(-50%, 0);
                        left: 50%;
                    }
                }

                @media (max-width: 570px) {
                    form{
                        padding: 0 1.5rem;
                    }

                    .image{
                        display: none;
                    }

                    .panel .content{
                        padding: 0.5rem 1rem;
                    }

                    .container:before{
                        bottom: 72%;
                        left: 50%;
                    }

                    .container.sign-up-mode:before{
                        bottom: 28%;
                        left: 50%;
                    }
                }
                /* -------------x------------- Responsive page -------------x-------------  */
            </style>
    </head>

    <body>
    {% block body %}

    <div class="container">
            <div class="forms-container">
                <div class="signin-signup">
                    <!-- ---------------------- Sign up form ---------------------- -->
                    <form class="sign-in-form" action="" method="post">
                        {% if error %}
                            <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                        {% endif %}

                        <h2 class="title">Prêt à se connecter</h2>
                        <div class="input-field">
                            <i class="fas fa-user"></i>
                            <input type="email" placeholder="Mail" name="email" required>
                        </div>
                        <div class="input-field">
                            <i class="fas fa-lock"></i>
                            <input type="password" placeholder="Mot de passe" name="password" required>
                        </div>
                        <label for=""><input type="checkbox" id="remember_me" name="_remember_me"> Rester connecté</label>
                        <input class="btn solid" type="submit" value="Connexion">
                        <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
                        <p class="social-text">Ou s'identifier avec les plateformes sociales</p>
                        <div class="social-media">
                            <a class="social-icon facebook" href="https://www.facebook.com/" target="_blank">
                                <i class="fab fa-facebook-f"></i>
                            </a>
                            <a class="social-icon twitter" href="https://twitter.com/" target="_blank">
                                <i class="fab fa-twitter"></i>
                            </a>
                            <a class="social-icon google" href="https://www.google.com/" target="_blank">
                                <i class="fab fa-google"></i>
                            </a>
                            <a class="social-icon linkedin" href="https://www.linkedin.com/in/matine-nassirpour-01ba831a6/" target="_blank">
                                <i class="fab fa-linkedin-in"></i>
                            </a>
                        </div>
                    </form>
                    <!-- ----------x----------- Sign in form ----------x----------- -->
                    <!-- ---------------------- Sign up form ---------------------- -->
                    {{ render(controller(
                        'App\\Controller\\RegistrationController::register'
                    )) }}
                    <!-- ----------x----------- Sign up form ----------x----------- -->
                </div>
            </div>
            <div class="panels-container">
                <!-- ---------------------- Left panel ---------------------- -->
                <div class="panel left-panel">
                    <div class="content">
                        <h3>Êtes-vous nouveau ?</h3>
                        <p>En devenant membre, vous accédez à des avantages exclusifs, à un coaching individuel et plus encore.</p>
                        <button class="btn transparent" id="sign-up-btn">S'inscrire</button>
                    </div>
                    <img src="{{ asset('build/images/unlock.svg') }}" class="image" alt="">
                </div>
                <!-- ----------x----------- Left panel ----------x----------- -->
                <!-- ---------------------- Right panel ---------------------- -->
                <div class="panel right-panel">
                    <div class="content">
                        <h3>Un de nous ?</h3>
                        <p>Êtes-vous prêt à vous mettre au défi ? Connectez-vous, de grands événements vous attendent !</p>
                        <button class="btn transparent" id="sign-in-btn">S'idnetifier</button>
                    </div>
                    <img src="{{ asset('build/images/Folderfiles.svg') }}" class="image" alt="">
                </div>
                <!-- ----------x----------- Right panel ----------x----------- -->
            </div>
        </div>



    {% block javascripts %}
            <!-- ----------- Font Awesome CDN ----------- -->
            <script src="https://kit.fontawesome.com/30428b0ab5.js" crossorigin="anonymous"></script>

            <script>
                const sign_in_btn = document.querySelector("#sign-in-btn");
                const sign_up_btn = document.querySelector("#sign-up-btn");
                const container = document.querySelector(".container");

                sign_up_btn.addEventListener('click', () => {
                    container.classList.add("sign-up-mode");
                });

                sign_in_btn.addEventListener('click', () => {
                    container.classList.remove("sign-up-mode");
                });
            </script>
        {% endblock %}

    {% endblock %}
    </body>
</html>

