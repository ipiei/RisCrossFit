<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">

        <link rel="icon" href="{{ asset('build/images/RisLogo1.png') }}">

        <link rel="stylesheet" href="//code.jquery.com/ui/1.12.1/themes/base/jquery-ui.css">
        <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
        {{ encore_entry_link_tags('app') }}
        <title>Découvrir l'événement</title>
    </head>

    {# --------------------- CSS Classes --------------------- #}
    <style>
        {# ----------------------------- Import fonts from google font ----------------------------- #}
        @import url('https://fonts.googleapis.com/css2?family=Lobster&family=Montserrat:wght@200;300;400;500;700;900&family=Open+Sans:wght@300;400;600;700;800&family=Roboto:wght@300;400;700;900&display=swap&family=Reem+Kufi&display=swap&family=Lexend+Deca&display=swap');

        :root{
            --primary-gradient: linear-gradient(to right, #4e4376, #2b5876);
            --mango-gradient: linear-gradient(to right, #ffa751, #ffe259);
            --text-color: #f7f9f9;
            --OpenSans: 'Open Sans', sans-serif;
            --Montserrat: 'Montserrat', cursive, sans-serif;
            --Lobster: 'Lobster', cursive, sans-serif;
            --Roboto: 'Roboto', sans-serif;
            --primary-shadow: 3px 4px 7px rgba(27, 27, 27, 0.534);
            --border-color: rgba(0, 0, 0, 0.329);
        }
        html, body{
            height: 100%;
            overflow: hidden;
            background: url({{ asset('build/images/BCMicrosoft.png') }}) no-repeat;
            background-size: cover;
        }

        .container{
            display: flex;
            justify-content: center;
            align-items: center;
            height: 98%;
            max-height: 610px;
            position: relative;
            top: 50%;
            transform: translateY(-50%);
        }
        .container .panel{
            width: 80%;
            height: 100%;
            /* box-shadow: var(--primary-gradient); */
        }
        .panel{
            box-shadow: 16px 4px 6px rgba(0, 0, 0, 0.2);
            /* box-shadow: 0 2px 6px rgba(0,0,0,.2); */
        }
        .row{
            display: grid;
            grid-template-columns: 60% 40%;
            height: 100%;
            background-color: #ffffff;
        }
        .liquid{
            background: url("{{ asset('uploads/images/'~evenement.image) }}") no-repeat center;
            background-size: cover;
        }
        .liquid #logo{
            width: 100px;
            margin-top: 18px;
            margin-left: 16px;
        }
        .table{
            padding: 20px;
        }
        .follow{
            display: flex;
            width: 40px;
            height: 350px;
            margin: auto;
            justify-content: center;
            border-radius: 15px;
        }
        .follow{
            list-style-type: none;
            margin: 0;
            padding: 0;
            position: absolute;
            align-items: center;
            text-align: center;
            transform: translate(150%, 96%);
        }
        .follow a{
            color: #ffffff;
            font-size: 18px;
        }
        /*.follow .fa-facebook-f{
            margin-left: 16px;
        }*/
        .follow a.facebook:hover{
            color: #1877f2;
        }
        .facebook i {
            padding: 5px;
            transition: all ease-in-out 150ms;
        }
        .facebook i:hover {
            margin-top: -3px;
            text-shadow: 0 14px 10px rgba(0, 0, 0, 0.4);
        }
        .follow a.instagram:hover{
            color: transparent;
            background: radial-gradient(circle at 30% 107%, #fdf497 0%, #fdf497 5%, #fd5949 45%, #d6249f 60%, #285AEB 90%);
            background: -webkit-radial-gradient(circle at 30% 107%, #fdf497 0%, #fdf497 5%, #fd5949 45%, #d6249f 60%, #285AEB 90%);
            -webkit-background-clip: text;
        }
        .instagram i {
            padding: 5px;
            transition: all ease-in-out 150ms;
        }
        .instagram i:hover {
            margin-top: -3px;
            text-shadow: 0 14px 10px rgba(0, 0, 0, 0.4);
        }
        .follow a.twitter:hover{
            color: #1da1f2;
        }
        .twitter i {
            padding: 5px;
            transition: all ease-in-out 150ms;
        }
        .twitter i:hover {
            margin-top: -3px;
            text-shadow: 0 14px 10px rgba(0, 0, 0, 0.4);
        }
        .follow a.whatsapp:hover{
            color: #00e676;
        }
        .whatsapp i {
            padding: 5px;
            transition: all ease-in-out 150ms;
        }
        .whatsapp i:hover {
            margin-top: -3px;
            text-shadow: 0 14px 10px rgba(0, 0, 0, 0.4);
        }
        .follow a.linkedin:hover{
            color: #0a66c2;
        }
        .linkedin i {
            padding: 5px;
            transition: all ease-in-out 150ms;
        }
        .linkedin i:hover {
            margin-top: -3px;
            text-shadow: 0 14px 10px rgba(0, 0, 0, 0.4);
        }
        .follow a:focus{
            outline: none;
        }
        .table .event-title h5{
            text-align: center;
            font-size: 18px;
        }
        .table .event-title p{
            text-align: justify;
        }
        .table .navigation{
            margin: 20px auto;
        }
        .table .navigation a{
            text-decoration: none;
            color: #1877f2;
        }
        .table .navigation a:hover{
            opacity: 0.6;
        }
    </style>
    {# -----------x--------- CSS Classes -----------x--------- #}

    <body>
        {# --------------------------- Event details --------------------------- #}
        <div class="container">
            <div class="panel">
                <div class="row">
                    <div class="col liquid">
                        <a href="{{ path('home') }}">
                            <img id="logo" src="{{ asset('build/images/RisLogo1.png') }}" alt="Logo">
                        </a>
                        <div class="follow">
                            <a class="facebook" href="https://www.facebook.com/" target="_blank">
                                <i class="fab fa-facebook-f"></i></a>
                            <a class="instagram" href="https://www.instagram.com/" target="_blank">
                                <i class="fab fa-instagram"></i></a>
                            <a class="twitter" href="https://twitter.com/?lang=fr" target="_blank">
                                <i class="fab fa-twitter"></i></a>
                            <a class="whatsapp" href="tel:+33603941673">
                                <i class="fab fa-whatsapp"></i></a>
                            <a class="linkedin" href="https://www.linkedin.com/in/matine-nassirpour-01ba831a6/" target="_blank">
                                <i class="fab fa-linkedin-in"></i></a>
                        </div>
                    </div>
                    <div class="col table">
                        <div class="event-title">
                            <h5>{{ evenement.titre }}</h5>
                            <p>{{ evenement.description }}</p>
                            <hr>
                            <h7>Lieu : </h7>
                            <p>{{ evenement.lieu }}</p>
                        </div>
                        <div class="date">
                            <h7>Date de début : {{ evenement.dateDebut ? evenement.dateDebut|date('d/m/Y') : '' }}</h7>
                            <br>
                            <h7>Date de fin : {{ evenement.dateFin ? evenement.dateFin|date('d/m/Y') : '' }}</h7>
                            <br>
                            <h7>Date fin d'inscription : {{ evenement.dateFinInscription ? evenement.dateFinInscription|date('d/m/Y') : '' }}</h7>
                        </div>
                        <div class="navigation">
                            <a href="{{ path('evenements') }}">Retour à la liste</a><br>
                            {% if is_granted('ROLE_ADMIN') == true %}
                                <a href="{{ path('evenement_edit', {'id': evenement.id}) }}">Modifier</a>
                            {% endif %}

                            {% if is_granted('ROLE_ADMIN') == true %}
                                {{ include('evenement/_delete_form.html.twig') }}
                            {% endif %}
                            {% if app.user is not null %}
                            <a href="{{ path('participer', {'id': evenement.id}) }}">Participer</a>
                            {% endif %}
                        </div>
                    </div>
                </div>
            </div>
        </div>
        {# ------------x-------------- Event details ------------x-------------- #}

        <script src="https://code.jquery.com/jquery-1.12.4.js"></script>
        <script src="https://code.jquery.com/ui/1.12.1/jquery-ui.js"></script>
        <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
        <script src="https://code.jquery.com/pep/0.4.3/pep.js"></script>
        {# --------------------------- Font Awesome CDN --------------------------- #}
        <script src="https://kit.fontawesome.com/30428b0ab5.js" crossorigin="anonymous"></script>
        {# ------------x-------------- Font Awesome CDN ------------x-------------- #}
        {{ encore_entry_script_tags('app') }}

        <script>
        </script>

    </body>
</html>




























{#
<h1>Evenement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Titre</th>
                <td>{{ evenement.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ evenement.description }}</td>
            </tr>
            <tr>
                <th>Lieu</th>
                <td>{{ evenement.lieu }}</td>
            </tr>
            <tr>
                <th>DateDebut</th>
                <td>{{ evenement.dateDebut ? evenement.dateDebut|date('d-m-Y') : '' }}</td>
            </tr>
            <tr>
                <th>DateFin</th>
                <td>{{ evenement.dateFin ? evenement.dateFin|date('d-m-Y') : '' }}</td>
            </tr>
            <tr>
                <th>DateFinInscription</th>
                <td>{{ evenement.dateFinInscription ? evenement.dateFinInscription|date('d-m-Y') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('evenements') }}">back to list</a>

    {% if is_granted('ROLE_ADMIN') == true %}
        <a href="{{ path('evenement_edit', {'id': evenement.id}) }}">edit</a>
    {% endif %}

    {% if is_granted('ROLE_ADMIN') == true %}
        {{ include('evenement/_delete_form.html.twig') }}
    {% endif %}
#}
